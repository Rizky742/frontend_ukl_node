import React, { useEffect, useState } from 'react'
import axios from 'axios'

export default function Coba() {
    let [woi,setWoi] = useState

    // useEffect(() => {
    //     axios.get('localhost:3380/send')
    //     .then(result => {
    //         console.log(result)
    //     })
    // },[])
  return (
    <div>sdfs</div>
  )
}
